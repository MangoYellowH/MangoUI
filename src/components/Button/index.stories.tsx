import React, { ReactElement } from 'react';
import Button from './index';

export default { title: 'Button1' };

export const ButtonStory = (): ReactElement => (
  <Button
    text="text 123"
  />
);
